rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /reqNot/actual {
      allow read, write: if request.auth != null
        && request.auth.token.email in ['xx@gmail.com', 'yy@gmail.com'];
    }
  }
}
